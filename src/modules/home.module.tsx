import { Hello } from "@components";
import React from "react";

export const HomeModule = (): JSX.Element => {
    return (
        <div>
            Home module
            <Hello />
        </div>
    );
};
